server:
  port: 59858

spring:
  application:
    name: haoyou-service-file
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
  web:
    resources:
      static-locations:
        - classpath:/static/
        - file:./data/upload/

haoyou:
  file:
    storage: local
    # 本机绝对路径：避免启动目录不同导致找不到已上传文件（图片 404）
    local-path: D:/zhuomian/好YOU经验知识共享平台/backend/data/upload
